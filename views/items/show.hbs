<nav>
    <div class="nav-wrapper">
      <a href="/{{user.username}}" class="brand-logo">Retro'd {{user.name}}</a>
      <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
      <ul class="right hide-on-med-and-down">
        <li><a href="/{{user.username}}">Retros</a></li>
        <li><a href="/{{user.username}}/{{retro._id}}">{{retro.title}}</a></li>
        <li><a href="/">Logout</a></li>
      </ul>
      <ul class="side-nav" id="mobile-demo">
        <li><a href="/{{user.username}}">Retros</a></li>
        <li><a href="/{{user.username}}/{{retro._id}}">{{retro.title}}</a></li>
        <li><a href="/">Logout</a></li>
      </ul>
    </div>
  </nav>


<h2>Action Item: {{item.title}}</h2>
<h4>Retro: {{retro.title}}</h4>
<a href="/{{user.username}}/{{retro._id}}/{{item._id}}/edit">Edit Action Item</a>
<a href="/{{user.username}}/{{retro._id}}/{{item._id}}/delete">Delete Action Item</a>
<h5>Description: </h5>
<p>{{item.description}}</p>

<ul>
    <li>Prioirty: {{item.priority}}</li>
    <li>Due Date: {{due}}</li>
    <li>Responsible: {{item.responsible}}</li>
    <li>Progress: {{item.progress}}</li>
</ul>