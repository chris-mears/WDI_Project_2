<nav>
  <div class="nav-wrapper">
    <a href="/{{user.username}}" class="brand-logo">Retro'd {{user.name}}</a>
    <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
    <ul class="right hide-on-med-and-down">
      <li><a href="/{{user.username}}">Retros</a></li>
      <li><a href="/{{user.username}}/{{retro._id}}">{{retro.title}}</a></li>
      <li><a href="/">Logout</a></li>
    </ul>
    <ul class="side-nav" id="mobile-demo">
      <li><a href="/{{user.username}}">Retros</a></li>
      <li><a href="/{{user.username}}/{{retro._id}}">{{retro.title}}</a></li>
      <li><a href="/">Logout</a></li>
    </ul>
  </div>
</nav>


<h3>Action Item: {{item.title}}</h3>
<h5>Retro: {{retro.title}}</h5>
<a class="tooltipped" data-position="bottom" data-delay="50" data-tooltip="Edit Item" href="/{{user.username}}/{{retro._id}}/{{item._id}}/edit"><i class="small material-icons">edit</i></a>

<a class="modal-trigger tooltipped" data-position="bottom" data-delay="50" data-tooltip="Delete item" href="#modal{{item._id}}"><i class="small material-icons">delete_forever</i></a>

<div id="modal{{item._id}}" class="modal">
  <div class="modal-content">
    <h5>Delete Action Item</h5>
    <p>Are you sure you want to Delete</p>
  </div>
  <div class="modal-footer">
    <a href="/{{user.username}}/{{retro._id}}/{{item._id}}/delete" class="modal-action modal-close waves-effect waves-green btn-flat"
      alt="Delete Retro">Agree</a>
  </div>
</div>

<h5>Description: </h5>
<p>{{item.description}}</p>

<ul>
  <li>Prioirty: {{item.priority}}</li>
  <li>Due Date: {{due}}</li>
  <li>Responsible: {{item.responsible}}</li>
  <li>Progress: {{item.progress}}</li>
</ul>