<h1>Welcome, {{user.name}} to your Retro's</h1>

<h2>Retros:</h2>
{{#each user.retros}}
    <div>
        <h2><a href="/{{../user.username}}/{{this._id}}">{{this.title}}</a></h2>
        <p>{{this.description}}</p>
        <p>Date: {{this.meetingDate}}</p>
        <p>Praticpents: {{this.participents}}</p>
    </div>
{{/each}}